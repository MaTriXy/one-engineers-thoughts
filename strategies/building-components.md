Here is a general outline of how this breaks down into subtasks:

```coffee
build components
  build component (general)
    outline dom elements used
    outline dummy values used
    write virtual-dom render method
    test in chrome
    write css for virtual-dom elements
      write css selectors
      write css selector properties
    match design to mockups exactly
    test in chrome
    outline properties used
    write properties on component
    wire up properties in component
    test in chrome
    outline functions needed for component
    write functions for component
    wire up functions in component
    test in chrome, firefox, and safari
  build overlay
  build modal sheet
  build form
  build button
  build text input
  build form field
  test with rainforest on chrome, firefox, safari, and ie10+
```
