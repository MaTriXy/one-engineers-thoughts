
```coffee
iterate range numbers
reload script
reload bashrc
convert string, to lowercase
convert string, to uppercase
redirect to stdout
append to stdout
pipe to stdout
pipe to stderr
print new line
alias a, b
escape quote single
loop line, in lines
exclude error, from command
check set variable
set variable, to output command
print directory current
run script executable
run script non executable
add to clipboard
pipe to clipboard
kill process child
pass paramters, to function
capture output multiline
find and replace
propagate arguments
check type variable
prompt for input
include script
compare string, string
diff tree(directory), tree(directory)
exit script
restart process, if dies
iterate over arguments
timeout command
generate dump core
set variable enviroment
trim whitespace
ensure only one script running at a time
parse xml
use variable, in command sed
send file, as email attachment
extract substring
match pattern inverse
capture output, into format
define hashtable
debug script
check if array contains value
detect if script is running through a pipe
create directory temporary
create nonexisting subdirectories recursively
create executable, in ocaml
read argv ocaml
```

```
loop until equal i, n
  increment i
```
